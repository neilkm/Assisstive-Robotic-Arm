#ifndef JETSON_ARM_UART_PACKET_H
#define JETSON_ARM_UART_PACKET_H

/*
 * Keep the Jetson protocol definition byte-for-byte aligned with the STM32
 * protocol definition while preserving the requested Jetson-local packet.h.
 */
#include "../../../STM32/include/packet.h"

#endif
